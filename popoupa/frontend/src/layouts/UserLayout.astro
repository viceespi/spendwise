---
import Button from "../components/Button.astro";
import type { User } from "../lib/models/user";
import { UserServices } from "../lib/services/user-service";

const userServices = new UserServices();
var currentUser = await userServices.getCurrentUser();
var demoUser: User = {
  userName: "Angus",
  userEmail: "Angus@barkmail.com",
  userId: "00c462c8-378f-4e26-9878-b5408c498e2c",
};
currentUser = demoUser;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Popoupa!</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body>
    <header>
      <Button
        buttonType="button"
        buttonName="Support"
        buttonClass="mainHeaderBtn"
        buttonId="support-button"
      />
      <div id="logo" class="lightText"><a href="/"><h1>PoPoupa</h1></a></div>
      <Button
        buttonType="button"
        buttonName={currentUser.userName}
        buttonClass="mainHeaderBtn"
        buttonId="user-button"
      />
    </header>
    <div id="main-space"><slot /></div>
  </body>
</html>

<style is:global>
  @import url("https://fonts.googleapis.com/css2?family=Amatic+SC&display=swap");

  html {
    background-color: #cf9f6e;
    z-index: -10;
    width: 100%;
    height: 100%;
    font-size: 18px;
  }

  header {
    display: flex;
    background-color: brown;
    height: 10%;
    border-radius: 10px;
    z-index: 10;
    justify-content: center;
    align-items: center;
    width: 100%;
    border: 1px solid black;
  }

  #logo {
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 2rem;
    flex-grow: 1;
    font-weight: bold;
    color: white;
  }

  #logo a {
    text-decoration: none;
    color: white;
  }

  #logo a:visited {
    text-decoration: none;
    color: white;
  }

  #logo h1 {
    margin: 0;
  }

  body {
    width: 90%;
    height: 100%;
    margin-left: 5%;
    margin-right: 5%;
    margin-top: 0%;
    margin-bottom: 0%;
    z-index: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
  }

  #main-space {
    width: 100%;
    height: 90%;
    border-radius: 10px;
    background-color: #652817;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .prettyText {
    font-family: "Amatic SC", sans-serif;
  }

  .darkText {
    font-family: "Amatic SC", sans-serif;
    color: black;
  }

  .lightText {
    font-family: "Amatic SC", sans-serif;
    color: white;
    text-shadow:
      -1px -1px 0 black,
      1px -1px 0 black,
      -1px 1px 0 black,
      1px 1px 0 black;
  }
</style>

<script>
  let aboutUsButton = document.getElementById("support-button");
  aboutUsButton?.addEventListener("click", () => {
    window.location.href = "/support";
  });

  let loginButton = document.getElementById("user-button");
  loginButton?.addEventListener("click", () => {
    window.location.href = "/userOptions";
  });
  // Aqui preciso mudar a funcionalidade do botão, para virar um menu de sanduíche com as opções desejadas
</script>
